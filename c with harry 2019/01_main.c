#include <stdio.h> // pre processor directive
int main()         // main is the name of the function and int is the integer which will return the function

{
    int ex;// int is data type and ex is variable
    ex = 4;
    printf("%d\n", ex);
    printf("Hello world\n");
    // printf// individual tokens
    // (// individual tokens
    //     "Hello world\n"// individual tokens
    //     )// individual tokens
    //     ;// individual tokens
    return 0; // means program successfully runs and return 0
}
