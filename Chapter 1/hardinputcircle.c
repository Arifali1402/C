#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;
    printf("the area of the circle is %f sq.m\n", pi * radius * radius);
    int height = 3;
    printf("vol of the cylinder is %f metre cube\n", pi*radius*radius*height);
    return 0;
}
