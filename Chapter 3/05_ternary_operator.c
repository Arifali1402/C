#include <stdio.h>

int main(){
    int a;
printf("enter a\n");
scanf("%d", &a);
//one linear or terniary operator
 (a < 5)? printf("a is less than 5\n") : printf("a is greater than 5\n"); // ? and : are called ternary operators
    return 0;
}